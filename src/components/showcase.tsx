"use client";

import { useState, useTransition } from "react";
import { motion } from "framer-motion";
import { ArrowDownRight, ArrowRight, ArrowUpRight, Camera, Check, Menu, Play, Search, Sparkles, Video, X } from "lucide-react";
import { submitBooking, submitCreatorApplication, type ActionState } from "@/app/actions";
import { brandNames, caseStudies, filterCreators, type Creator } from "@/lib/data";

const niches = ["All", "Fashion", "Tech", "Lifestyle", "Gaming"] as const;
const platforms = ["All", "TikTok", "Instagram", "YouTube"] as const;

function Button({ children, onClick, type = "button", variant = "lime" }: { children: React.ReactNode; onClick?: () => void; type?: "button" | "submit"; variant?: "lime" | "ghost" }) {
  return <button type={type} onClick={onClick} className={`group inline-flex items-center justify-center gap-3 rounded-full px-5 py-3 text-sm font-bold transition-transform hover:-translate-y-0.5 ${variant === "lime" ? "bg-[#ccff00] text-[#0d0d0d] hover:bg-white" : "border border-white/20 bg-white/5 text-white hover:border-[#ccff00] hover:text-[#ccff00]"}`}>{children}</button>;
}

function SocialIcon({ platform }: { platform: string }) {
  if (platform === "Instagram") return <Camera className="size-3.5" />;
  if (platform === "YouTube") return <Video className="size-3.5" />;
  return <span className="text-[10px] font-black">TK</span>;
}

function CreatorCard({ creator, onAdd }: { creator: Creator; onAdd: (creator: Creator) => void }) {
  return <motion.article layout initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="group relative overflow-hidden rounded-[2px] border border-white/10 bg-[#171717]">
    <div className="relative aspect-[4/5] overflow-hidden"><img src={creator.image} alt={creator.name} className="size-full object-cover grayscale transition duration-700 group-hover:scale-105 group-hover:grayscale-0" /><div className="absolute inset-0 bg-gradient-to-t from-black via-black/5 to-transparent" /><div className="absolute left-4 top-4 flex items-center gap-1 rounded-full bg-black/60 px-3 py-1 text-[10px] uppercase tracking-[0.2em] text-white/80 backdrop-blur"><span className="size-1.5 rounded-full bg-[#ccff00]" /> roster</div><div className="absolute bottom-4 left-4 right-4"><div className="mb-3 flex items-center justify-between"><span className="rounded-full px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-black" style={{ backgroundColor: creator.accent }}>{creator.niche}</span><span className="text-xs text-white/65">{creator.location}</span></div><h3 className="text-2xl font-black tracking-tight text-white">{creator.name}</h3><p className="text-sm text-white/50">{creator.handle}</p></div></div>
    <div className="grid grid-cols-2 divide-x divide-white/10 border-t border-white/10"><div className="p-4"><p className="text-[10px] uppercase tracking-widest text-white/35">total reach</p><p className="mt-1 text-lg font-bold text-white">{creator.reach}</p></div><div className="p-4"><p className="text-[10px] uppercase tracking-widest text-white/35">engagement</p><p className="mt-1 text-lg font-bold text-white">{creator.engagement}</p></div></div>
    <div className="flex items-center justify-between p-3"><div className="flex gap-2 text-white/55">{creator.platforms.map((platform) => <span key={platform} title={platform} className="flex size-7 items-center justify-center rounded-full border border-white/10"><SocialIcon platform={platform} /></span>)}</div><button onClick={() => onAdd(creator)} className="flex items-center gap-2 text-xs font-bold text-[#ccff00] transition hover:text-white">add to brief <ArrowRight className="size-3 transition group-hover:translate-x-1" /></button></div>
  </motion.article>;
}

function BookingModal({ initialCreator, onClose }: { initialCreator?: Creator; onClose: () => void }) {
  const [mode, setMode] = useState<"brand" | "creator">("brand");
  const [pending, startTransition] = useTransition();
  const [state, setState] = useState<ActionState>({ status: "idle" });
  const [form, setForm] = useState({ name: "", email: "", company: "", brief: initialCreator ? `I want to build a campaign with ${initialCreator.name}.` : "", handle: "", niche: "Fashion" });
  const change = (key: string, value: string) => setForm((current) => ({ ...current, [key]: value }));
  const submit = (event: React.FormEvent) => { event.preventDefault(); startTransition(async () => { const result = mode === "brand" ? await submitBooking(state, form) : await submitCreatorApplication(state, form); setState(result); }); };
  return <div className="fixed inset-0 z-50 flex items-end justify-center overflow-y-auto bg-black/80 p-4 backdrop-blur-sm sm:items-center"><motion.div initial={{ opacity: 0, y: 30 }} animate={{ opacity: 1, y: 0 }} className="max-h-[calc(100dvh-2rem)] w-full max-w-xl overflow-y-auto border border-white/15 bg-[#171717] p-6 shadow-2xl sm:p-8"><div className="mb-8 flex items-start justify-between"><div><p className="mb-2 text-xs uppercase tracking-[0.25em] text-[#ccff00]">start something loud</p><h2 className="text-3xl font-black tracking-tight">Let&apos;s make a scene.</h2></div><button onClick={onClose} aria-label="Close booking form" className="text-white/50 hover:text-white"><X /></button></div><div className="mb-6 flex gap-2 border-b border-white/10 pb-3"><button onClick={() => { setMode("brand"); setState({ status: "idle" }); }} className={`px-1 text-sm font-bold ${mode === "brand" ? "text-[#ccff00]" : "text-white/40"}`}>I&apos;m a brand</button><span className="text-white/15">/</span><button onClick={() => { setMode("creator"); setState({ status: "idle" }); }} className={`px-1 text-sm font-bold ${mode === "creator" ? "text-[#ccff00]" : "text-white/40"}`}>I&apos;m a creator</button></div>{state.status === "success" ? <div className="py-12 text-center"><div className="mx-auto mb-4 flex size-14 items-center justify-center rounded-full bg-[#ccff00] text-black"><Check /></div><h3 className="text-2xl font-bold">You&apos;re on our radar.</h3><p className="mx-auto mt-3 max-w-sm text-sm text-white/55">{state.message}</p><button onClick={onClose} className="mt-8 text-sm font-bold text-[#ccff00]">close window</button></div> : <form onSubmit={submit} className="grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-xs uppercase tracking-widest text-white/45">name<input value={form.name} onChange={(e) => change("name", e.target.value)} className="field" placeholder="Your name" />{state.fields?.name && <span className="text-[#ff007f] normal-case tracking-normal">{state.fields.name}</span>}</label><label className="grid gap-2 text-xs uppercase tracking-widest text-white/45">email<input value={form.email} onChange={(e) => change("email", e.target.value)} className="field" placeholder="you@company.com" />{state.fields?.email && <span className="text-[#ff007f] normal-case tracking-normal">{state.fields.email}</span>}</label>{mode === "brand" ? <><label className="grid gap-2 text-xs uppercase tracking-widest text-white/45">company<input value={form.company} onChange={(e) => change("company", e.target.value)} className="field" placeholder="Brand or company" /></label><label className="grid gap-2 text-xs uppercase tracking-widest text-white/45">what are we making?<input value={form.brief} onChange={(e) => change("brief", e.target.value)} className="field" placeholder="Campaign, launch, social series..." /></label></> : <><label className="grid gap-2 text-xs uppercase tracking-widest text-white/45">social handle<input value={form.handle} onChange={(e) => change("handle", e.target.value)} className="field" placeholder="@yourhandle" /></label><label className="grid gap-2 text-xs uppercase tracking-widest text-white/45">your lane<select value={form.niche} onChange={(e) => change("niche", e.target.value)} className="field"><option>Fashion</option><option>Tech</option><option>Lifestyle</option><option>Gaming</option></select></label></>}<div className="sm:col-span-2">{state.message && <p className="mb-3 text-sm text-[#ff007f]">{state.message}</p>}<Button type="submit">{pending ? "sending..." : mode === "brand" ? "send the brief" : "apply to roster"}<ArrowRight className="size-4" /></Button></div></form>}</motion.div></div>;
}

export function Showcase() {
  const [niche, setNiche] = useState<string>("All");
  const [platform, setPlatform] = useState<string>("All");
  const [query, setQuery] = useState("");
  const [modal, setModal] = useState<{ creator?: Creator } | null>(null);
  const visibleCreators = filterCreators({ niche, platform, query });
  return <main className="overflow-hidden bg-[#0d0d0d] text-white"><header className="fixed left-0 right-0 top-0 z-40 border-b border-white/10 bg-[#0d0d0d]/85 backdrop-blur-xl"><nav className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 lg:px-8"><a href="#top" className="text-lg font-black tracking-[-0.08em]">PRO<span className="text-[#ccff00]">GANDA</span><span className="ml-1 text-[#ff007f]">.</span></a><div className="hidden items-center gap-8 text-xs font-bold uppercase tracking-widest text-white/55 md:flex"><a href="#creators" className="hover:text-white">Creators</a><a href="#studio" className="hover:text-white">Studio</a><a href="#work" className="hover:text-white">Work</a></div><Button onClick={() => setModal({})} variant="ghost">book a project <ArrowDownRight className="size-4" /></Button><button className="md:hidden" aria-label="Open menu"><Menu /></button></nav></header>
    <section id="top" className="relative mx-auto flex min-h-[760px] max-w-7xl items-end px-5 pb-20 pt-32 lg:px-8"><div className="pointer-events-none absolute -right-40 top-20 size-[620px] rounded-full bg-[#8a2be2]/20 blur-[130px]" /><div className="pointer-events-none absolute left-1/3 top-32 size-2 rounded-full bg-[#ccff00] shadow-[0_0_30px_12px_#ccff00]" /><div className="relative max-w-5xl"><motion.p initial={{ opacity: 0, y: 15 }} animate={{ opacity: 1, y: 0 }} className="mb-7 flex items-center gap-3 text-xs font-bold uppercase tracking-[0.28em] text-[#ccff00]"><Sparkles className="size-4" /> full-service creator agency + production house</motion.p><motion.h1 initial={{ opacity: 0, y: 25 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .1 }} className="max-w-5xl text-[clamp(4rem,11vw,10.5rem)] font-black uppercase leading-[.82] tracking-[-0.09em]">Make noise.<br /><span className="text-[#ccff00]">Move culture.</span></motion.h1><div className="mt-10 flex flex-col gap-8 lg:flex-row lg:items-end lg:justify-between"><p className="max-w-md text-lg leading-relaxed text-white/55">We match brands with the right voices, then bring the whole thing to life in-house. Strategy, talent, shoot, edit, distribution. No handoffs. No beige content.</p><div className="flex flex-wrap gap-3"><Button onClick={() => document.getElementById("creators")?.scrollIntoView({ behavior: "smooth" })}>explore creators <ArrowRight className="size-4" /></Button><Button onClick={() => setModal({})} variant="ghost">book a shoot</Button></div></div></div></section>
    <div className="border-y border-white/10 bg-[#ccff00] py-3 text-black"><div className="mx-auto flex max-w-7xl items-center justify-between gap-6 overflow-hidden px-5 text-xs font-black uppercase tracking-[0.2em] lg:px-8"><span>12.4B+ views generated</span><span className="hidden sm:block">500+ campaigns shipped</span><span>100% in-house production</span></div></div>
    <section id="creators" className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><div className="mb-12 flex flex-col justify-between gap-6 lg:flex-row lg:items-end"><div><p className="mb-3 text-xs font-bold uppercase tracking-[0.25em] text-[#ff007f]">01 / the roster</p><h2 className="max-w-2xl text-5xl font-black tracking-[-0.06em] sm:text-7xl">People with<br /><span className="text-white/35">something to say.</span></h2></div><p className="max-w-xs text-sm leading-relaxed text-white/45">A hand-picked roster of culture-shapers, built for attention and backed by data.</p></div><div className="mb-8 flex flex-col gap-4 border-y border-white/10 py-4 lg:flex-row lg:items-center lg:justify-between"><div className="flex gap-2 overflow-x-auto pb-1">{niches.map((item) => <button key={item} onClick={() => setNiche(item)} className={`whitespace-nowrap rounded-full px-4 py-2 text-xs font-bold ${niche === item ? "bg-white text-black" : "text-white/45 hover:text-white"}`}>{item}</button>)}</div><div className="flex gap-3"><label className="relative"><Search className="absolute left-3 top-2.5 size-3.5 text-white/35" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="search talent" className="h-9 w-40 border-b border-white/20 bg-transparent pl-8 text-xs outline-none focus:border-[#ccff00]" /></label><select value={platform} onChange={(e) => setPlatform(e.target.value)} className="h-9 border-b border-white/20 bg-[#0d0d0d] text-xs text-white/60 outline-none"><option value="All">all platforms</option>{platforms.slice(1).map((item) => <option key={item}>{item}</option>)}</select></div></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{visibleCreators.map((creator) => <CreatorCard key={creator.id} creator={creator} onAdd={(selected) => setModal({ creator: selected })} />)}</div>{visibleCreators.length === 0 && <p className="py-16 text-center text-white/45">No creators match that search yet.</p>}</section>
    <section id="studio" className="border-y border-white/10 bg-[#151515] px-5 py-24 lg:px-8"><div className="mx-auto max-w-7xl"><div className="grid gap-12 lg:grid-cols-[.8fr_1.2fr]"><div><p className="mb-3 text-xs font-bold uppercase tracking-[0.25em] text-[#ccff00]">02 / the studio</p><h2 className="text-5xl font-black tracking-[-0.06em] sm:text-7xl">From spark<br />to <span className="text-[#ff007f]">screen.</span></h2><p className="mt-7 max-w-sm text-sm leading-relaxed text-white/45">The whole production is ours. We build the brief, cast the perfect chemistry, shoot it, cut it, and send it into the world.</p></div><div className="grid gap-px border border-white/10 bg-white/10 sm:grid-cols-2">{["Creative concept", "Talent matching", "Studio + location", "Edit + post", "Distribution", "Analytics loop"].map((step, index) => <div key={step} className="group bg-[#151515] p-6 transition hover:bg-[#202020]"><span className="text-xs text-[#ccff00]">0{index + 1}</span><h3 className="mt-10 text-xl font-bold">{step}</h3><ArrowDownRight className="mt-8 size-5 text-white/20 transition group-hover:text-[#ccff00]" /></div>)}</div></div><div className="mt-20 grid gap-6 lg:grid-cols-[1.3fr_.7fr]"><div className="relative flex min-h-[340px] items-end overflow-hidden bg-[url('https://images.unsplash.com/photo-1492619375914-88005aa9e8fb?w=1400&q=85&auto=format&fit=crop')] bg-cover bg-center p-7"><div className="absolute inset-0 bg-gradient-to-t from-black/90 to-transparent" /><div className="relative"><span className="mb-3 inline-flex items-center gap-2 text-xs uppercase tracking-widest text-[#ccff00]"><span className="size-2 animate-pulse rounded-full bg-[#ff007f]" /> now playing</span><h3 className="text-3xl font-black">The VANTA cut / 01:24</h3></div><button aria-label="Play showreel" className="absolute right-7 top-7 flex size-14 items-center justify-center rounded-full bg-[#ccff00] text-black"><Play className="ml-1 fill-current" /></button></div><div className="flex flex-col justify-between border border-white/10 p-7"><p className="text-xs uppercase tracking-widest text-white/40">the output</p><p className="text-6xl font-black text-[#ccff00]">6.2x</p><p className="max-w-xs text-sm text-white/45">average campaign ROI for brands that trust the full pipeline.</p><ArrowRight className="size-6 text-[#ff007f]" /></div></div></div></section>
    <section id="work" className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><div className="mb-12 flex items-end justify-between"><div><p className="mb-3 text-xs font-bold uppercase tracking-[0.25em] text-[#8a2be2]">03 / selected work</p><h2 className="text-5xl font-black tracking-[-0.06em] sm:text-7xl">Receipts.</h2></div><span className="hidden text-sm text-white/35 sm:block">brands we&apos;ve made loud</span></div><div className="mb-16 flex gap-12 overflow-hidden border-y border-white/10 py-7 text-2xl font-black tracking-tight text-white/25 sm:text-4xl">{brandNames.map((brand) => <span key={brand} className="shrink-0">{brand}</span>)}</div><div className="grid gap-4 lg:grid-cols-3">{caseStudies.map((study) => <article key={study.brand} className="group border border-white/10 p-6 transition hover:border-white/35"><div className="mb-20 flex items-start justify-between"><span className="text-3xl font-black" style={{ color: study.color }}>{study.brand}</span><ArrowUpRight className="size-5 text-white/30" /></div><p className="text-xs uppercase tracking-widest text-white/40">{study.category}</p><h3 className="mt-2 text-2xl font-bold">{study.creator}</h3><div className="mt-8 grid grid-cols-2 border-t border-white/10 pt-4"><div><p className="text-[10px] uppercase tracking-widest text-white/35">views</p><p className="mt-1 text-xl font-bold">{study.views}</p></div><div><p className="text-[10px] uppercase tracking-widest text-white/35">roi</p><p className="mt-1 text-xl font-bold" style={{ color: study.color }}>{study.roi}</p></div></div></article>)}</div></section>
    <section className="mx-auto max-w-7xl px-5 pb-24 lg:px-8"><div className="relative overflow-hidden bg-[#ccff00] p-8 text-black sm:p-14"><div className="absolute -right-8 -top-20 text-[220px] font-black leading-none opacity-10">+</div><p className="relative mb-4 text-xs font-black uppercase tracking-[0.25em]">your next big thing</p><h2 className="relative max-w-3xl text-5xl font-black leading-[.9] tracking-[-0.06em] sm:text-8xl">Make the thing<br />people can&apos;t scroll past.</h2><button onClick={() => setModal({})} className="relative mt-10 inline-flex items-center gap-3 rounded-full bg-black px-6 py-4 text-sm font-bold text-white hover:bg-[#ff007f]">start a conversation <ArrowRight className="size-4" /></button></div></section><footer className="border-t border-white/10 px-5 py-8 lg:px-8"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-4 text-xs text-white/35 sm:flex-row"><span className="font-black tracking-[-0.05em] text-white">PROGANDA<span className="text-[#ccff00]">.</span></span><span>creative work for the attention economy / 2026</span><span>hello@proganda.studio</span></div></footer>{modal && <BookingModal initialCreator={modal.creator} onClose={() => setModal(null)} />}</main>;
}